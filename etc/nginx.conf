include       mime.types;
default_type  application/octet-stream;
server {
    listen       80 default;

    location /uploads/ {
        root /home/box/web;
    }

    location ~ \.\w\w\w$ {
        root /home/box/web/public;
    }

    location / {
      return 404;
        }
}
